---
ansible-navigator:
  ansible:
    config:
      help: true
  ansible-builder:
    help: true
  execution-environment:
    pull:
      policy: never
    volume-mounts:
    - dest: /tmp/foo
      options: Z
      src: /tmp/foo
  playbook-artifact:
    save-as: '{playbook_dir}/{playbook_name}-artifact-{time_stamp}.json'
