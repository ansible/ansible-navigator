---
ansible-navigator:
  cmdline: "--forks 15"
  editor:
    command: test_command
    console: False
  documentation:
    plugin:
      name: shell
      type: become
  execution-environment:
    container-engine: podman
    enabled: False
    environment-variables:
      pass:
        - ONE
        - TWO
        - THREE
      set:
        KEY1: VALUE1
        KEY2: VALUE2
        KEY3: VALUE3
    image: test_image
  inventories:
    - /tmp/test_inventory.yml
  inventory-columns:
    - ansible_network_os
    - ansible_network_cli_ssh_type
    - ansible_connection
  logging:
    level: critical
    file: /tmp/log.txt
  mode: stdout
  osc4: False
  playbook: /tmp/test_playbook.yml
  playbook-artifact: test_artifact.json
