---
name: push
on:
  push:
    branches:
      - main
      - "releases/**"
      - "stable/**"

permissions:
  contents: read

jobs:
  ack:
    uses: ansible/team-devtools/.github/workflows/push.yml@main
